echo "==========================================="
echo "==              FAST NETWORK            ==="
echo "==========================================="

mv sysctl.conf /etc/sysctl.conf
sudo sysctl -p
